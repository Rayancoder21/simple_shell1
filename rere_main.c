#include "Main.h"
/*
* main - entry point
* Return: nothing
**/
int main(void)
{
	char *args[128];

	while (true)
	{
		rere_infinite_prompt();
		Rere_executeCommand(const char *command);
		re_re_input(char *rere, size_t size);
	}
	return (0);
}
